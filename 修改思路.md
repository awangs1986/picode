# Picode Next 第二—第三轮架构修改思路

> 配套主文档：`PICODE-NEXT-MASTER-ARCHITECTURE.md`  
> 日期：2026-08-04  
> 目的：向架构师解释第二轮修订的取舍，而不是另行定义一个产品

## 1. 本轮修改的总体判断

我们接受第一轮评审的主要批评：原稿的权威分配和 Seam 设计是正确的，但安全评审历史占据了过多实施预算，长任务抗失真与真实开发速度反而不够突出。

我们不接受“再做一个 Mini/MVP 产品”的表述。Picode V2 已经证明账号、聊天、GUI、扩展、模型和备份等完整产品能力可行；新版本的目标是用更清晰的权威模型重构这些能力，而不是另开一个功能不足的分支。

因此本轮采用三项调整：

1. P0–P4 仍然交付完整产品，但把高级对抗安全和手机/远程能力移入 P5。
2. 加入正式 P0 前的 Bridge Feasibility Spike，先验证最危险的技术假设。
3. 把 Task Slice、Task Capsule、Developer TDD、Verification Budget 和 QA Handoff 提升为核心设计。

---

## 2. 降低安全复杂度的思路

### 2.1 为什么原设计会过重

V2 经历过 R1–R6 连续红队评审。每一轮都针对上轮规则寻找新的组合攻击链，因此逐渐增加了内容 Hash Permit、TOCTOU、防 mmap/继承 FD、文件别名围栏、CI OIDC、Host-signed Parser/Inspector、Shared Base Registry 等机制。

这些机制在多租户、运行不可信第三方 Agent 或远程团队平台里合理，但 Picode P0–P4 的真实用户是个人开发者：用户选择自己的模型，在自己的机器上修改自己的项目。主要风险是误操作、越界写入、误 push、弱化测试、泄露秘密和进程失控，而不是主动攻击者持续伪造每个信任根。

原稿没有区分“理论上还能被攻击”和“个人开发产品值得承担的复杂度”，导致安全 Implementation 的成本超过了 TDD 与上下文治理本身。

### 2.2 新的安全复杂度上限

我们采用一个可执行的判断标准：

> Picode 的通用安全能力以固定版本的 Grok Build 为复杂度基线。超过 Grok Build 的安全保证默认移入 P5；只有它直接保护 Picode 的 TDD 正确性、用户文件或 Git 所有权，并且 Implementation 与心智成本足够低时，才允许进入 P0–P4。

这里比较的是能力和复杂度，不是宣传文案。实施时必须固定被比较的 Grok Build Commit，并记录它的实际机制、许可证和平台差异。

### 2.3 P0–P4 保留什么

- Prompt 层的可逆操作与秘密保护提示；
- Shell、文件、Git 和已知外部操作的 `allow / ask / deny`；
- 允许一次、允许精确命令/规则、全局允许；
- Plan Mode 只读；
- Agent 未经授权不能 commit、merge、push、改写历史或发布；
- 用户批准时记录命令、目标和被执行脚本的轻量 Fingerprint；启动前内容变化则重新询问；
- Managed Worktree、规范化路径、用户 dirty/untracked 保护；
- Secret Reference、日志脱敏；
- Work 的取消、超时、预算和进程树回收；
- Linux/macOS 可复用的成熟沙箱能力；
- 模型和扩展不能替用户授权，也不能用文字代替实际 Gate Result。

这些能力覆盖个人开发者绝大多数真实风险，而且不会把普通工具调用变成复杂的安全协议。

### 2.4 哪些能力进入 P5

- 密码学单次 Permit、跨 Actor 重放防御和完整 TOCTOU 协议（不包含上面的低成本批准 Fingerprint）；
- TOCTOU、hardlink/junction/ADS、mmap 和现有 FD 防御；
- Host-signed Parser/Inspector/Challenge Registry；
- Shared Base Registry 与 cache poisoning 对抗；
- CI OIDC、签名 Evidence 和伪造 webhook 防御；
- 恶意 Manifest/Extension fuzzing；
- 完整外部 Publication Effect 对抗；
- Windows 原生强沙箱；
- 手机、远程控制和跨设备 Writer Lease。

这些设计没有被否定。V2 R1–R6 文档继续作为 P5 Hardened Profile 的输入，但不再阻塞 P0–P4 的完整产品。

### 2.5 为什么 Developer TDD 可以比 Grok 更严格

TDD 状态机、RED/GREEN、Candidate Snapshot 和 Integration Gate 不属于“为了防攻击而增加的安全层”，而是 Picode 的核心产品正确性。它们可以比 Grok Build 更强，但必须保持克制：

- 只验证当前目标和受影响范围；
- 新 Gate 证明一次能红，不在每次运行做昂贵 Challenge；
- 默认两轮自动修复、一轮 LLM Review、一次 Integration Smoke；
- 达到时间/Token/轮次预算后停止；
- 无法机器稳定判断的内容转成 QA Risk；
- 用户可以按交付时间接受已知风险，系统准确标注。

这让 TDD 提高可信度，而不是成为无限循环的安全审判。

---

## 3. 七个 Module 为什么会被误解

### 3.1 误解是什么

第一轮评审把“七个 Module 的每个 Interface 都需要序列化、版本协商和 IPC”作为双语言成本的一部分。这说明原文的图容易让读者把 Module 理解为七个微服务或七个独立进程。

这不是原设计的实际含义。

### 3.2 实际运行形态

```text
Upstream Pi Runtime / Pi TUI
              │
              │ 唯一主要跨语言 IPC Seam
              ▼
Thin TypeScript Pi Bridge
              │
              ▼
One Rust Foreground Host Process
 ├─ Session Gateway
 ├─ Context & Memory
 ├─ Task Control
 ├─ Capability & Tool Catalog
 ├─ Authorization & Policy
 ├─ Work & Sandbox
 └─ Verification
```

七个 Module 位于同一个 Rust Host 进程。它们通常通过进程内 Rust 调用协作，不需要七套网络协议，也不是七个 daemon。跨语言的主要 Seam 只有 Pi Runtime ↔ Thin Bridge ↔ Rust Host。

### 3.3 “七个”的真正意义

七个 Module 是七类唯一权威：

- Chat/Session 事实不能散落在 GUI、TUI 和数据库扫描器里；
- Context 不能由每个扩展自行拼接；
- Task 完成不能由 Prompt、Todo 或模型文字分别决定；
- Extension 生命周期不能由 MCP/LSP/DAP 各自维护；
- Policy 不能在 Shell、Git 和 Hook 中各写一套；
- Work 进程不能由每个插件自行守护；
- Gate Result 不能由开发 Agent 自己宣布。

Module 的目的在于 Depth、Leverage 和 Locality，而不是部署隔离。一个 Module 可以只是 Rust crate 内一个深状态机；Interface 也包含不变量、顺序、错误和性能语义，不等于远程 API。

### 3.4 不会一次性搭七个空壳

第二轮路线不再要求 P0 先建立七个完整 Interface skeleton。每个阶段从真实纵向流程加深所需 Module：

- P0 只建立 Pi Seam、最小生命周期和 Simple；
- P1 加深 Context、Task、Policy、Work；
- P2 加深 Verification、Worktree 和 Subagent 集成；
- P3 加深 Session、Capability、账号与扩展；
- P4 通过 GUI 和真实项目验证所有 Interface 的 Leverage。

如果删除一个 Module 后复杂性不会散落到多个调用者，说明它是浅 Module，应合并 Implementation，而不是为了架构图保留。

### 3.5 为什么仍然需要 Rust

我们没有采纳“先把七个 Module 全写成 TypeScript，以后再迁 Rust”的方案，因为这会制造第二套临时 V2。更合理的语言分工是：

- TypeScript：最薄 Pi Extension、TUI/lifecycle glue 和必须运行在 Pi 内部的 Adapter；
- Rust：Policy、进程监督、Worktree、Gate Runner、SQLite 控制面和七类领域权威；
- 上游 Pi：模型调用、Agent Loop、原生 Session、TUI 和 Tool Result 回灌。

Bridge Spike 会先证明这个分工是否成立。如果 Pi 公开 Interface 不足，再决定最小 Pi Patch，而不是先假设 Rust 能拦截一切。

---

## 4. 为什么 Picode 不是游戏引擎

### 4.1 游戏是业务场景，不是领域核心

用户从事游戏开发，因此 Picode 必须能承受大型仓库、长任务、多 Module、复杂构建、跨模块测试和发布日期压力。这决定了 Task Slice、Worktree、Developer TDD 和 QA Handoff 的设计。

但 Picode 本身不是游戏引擎，也不应把 Unity、Unreal、Godot 或某个自研引擎的对象模型写进核心。它服务的是开发流程，不是运行时、渲染、物理或关卡编辑领域。

### 4.2 核心保持通用

核心 Verification 只认识：

- Gate Contract；
- 命令与结果；
- Candidate Snapshot；
- Module/Contract Edge；
- Verification Budget；
- Completion Label 与 QA Handoff。

它不假设什么是场景、Actor、Prefab、Component、Frame、Replay 或 Golden Image。

### 4.3 游戏特有验证作为 Adapter

以下能力可以组成 Tier 3 `Game Verification Pack`：

- 无头运行器；
- 固定时间步和随机种子；
- 回放一致性；
- 黄金图像/快照；
- 性能与帧时间采样；
- 引擎测试命令 Adapter。

这些 Adapter 把引擎结果翻译为通用 Gate Result。用户不启用时，它们没有进程、模型可见性或核心成本。其他软件项目也可以提供自己的领域 Verification Pack。

Tier 3 表示默认可见性和运行状态，不等于不计入日程。若 P2 真实项目实验选用游戏仓库，P2 必须完成该项目所需的最小 Adapter；优先调用仓库已有 headless/test/CI 命令。只有数据证明可跨项目复用后，P4 才泛化为正式 Game Verification Pack。

### 4.4 为什么仍用真实游戏项目验收

真实游戏项目是压力测试场景，因为它同时具有长上下文、跨 Module、构建昂贵、测试不完全确定和发布日期约束。P4 会在真实中型软件或游戏项目中记录：

- Task Slice 数量与长度；
- Context 大小和目标漂移；
- Gate 真失败、假失败和 flake；
- 自动修复轮数；
- 人工纠正次数；
- QA Handoff 质量；
- 交付耗时。

这是验证通用 Harness 是否有效，不是把产品变成游戏引擎工具。

---

## 5. 其他关键修订

### 5.1 不创建 Mini/MVP 分支

P0–P4 是完整 Picode 的重构路线。P2 结束就必须在真实中型仓库验证 Slice/Capsule；P3 再以 A（Provider/Account/Model）、B（Capability/Extension）、C（Session Productization）三个独立子 Gate 迁移 V2 完整能力。P4 完成 GUI、Runtime 监看、自动化测试并扩大真实项目样本。P5 只是高级安全、Windows 强沙箱与远程能力，不是“剩下的完整产品”。

### 5.2 Bridge Spike 提前

Bridge 是全局最高风险假设，所以正式 P0 前验证 Pi 原生工具、扩展、MCP、Hook、Subagent、取消和 Tool Result，同时补充四个会推翻拓扑的条件：compaction 的观察/请求、rewind/fork 事件、Rust Host 包裹原版 TUI 时的 PTY/信号/resize 所有权，以及每个 Tool Intent 往返的 p50/p95 延迟。输出明确为：无需 Patch、最小可维护 Patch，或当前 Interface 不可行。

### 5.3 Task Slice 成为抗失真主机制

长 Task 不只依靠 compaction。Task Control 独占 Capsule 生命周期和事实内容，Context & Memory 只负责渲染。Capsule 强制分为从权威源复制、禁止摘要的 Verbatim Facts，以及允许有来源摘要的 Narrative。下一 Slice 的 Required Context 从文件/Git/Gate Result 重新推导，不能只信摘要链。

模型可以提议切片，Task Control 根据阶段完成、Context 占用、轮次/Token 预算、compaction 临界点和账号接续强制决定，用户可以否决。系统记录切片原因和缓存代价，用真实项目校准阈值。

### 5.4 QA 与开发循环分离

Picode 负责有限 Developer TDD，不替代 QA。测试意外失败时只在相同 Snapshot/命令/环境下确认重跑一次；无代码变化而转绿则标记 `Flaky`，不消耗修复轮次、不算稳定 GREEN，并进入 QA Risk。QA 发现问题后创建新的缺陷 Slice，而不是让 QA Agent 和开发 Agent 在同一超长上下文中无限互改。

---

## 6. 第三轮对专家建议的取舍

以下建议在用户确认后已采用：

- 批准后的命令/脚本内容发生变化时重新询问，但不扩展成完整密码学 Permit；
- Task Control 成为 Capsule 唯一权威，Context 只渲染；
- Bridge Spike 增加 compaction、rewind/fork、终端所有权和延迟；
- Capsule 使用 Verbatim Facts / Narrative 分区；
- 模型提议 Slice、Task Control 按预算信号决定、用户可否决；
- Flaky 成为 P2 状态，不消耗修复轮次；
- P2 结束提前做真实项目漂移实验；
- P3 拆成 A/B/C 三个独立子 Gate；
- onboarding 多客户端 Wizard Lease 简化为单机 lockfile；
- P0–P4 的 Capability Epoch 简化为 Tool Schema Digest 对比；
- 外部聊天导入安排在 P3-C 后半段，各来源 Adapter 独立验收。

两项没有照单全收：

1. **Extension 四态不降成 Enabled/Disabled。** `Enabled ≠ Trusted ≠ Running` 是此前明确的领域事实。Implementation 可以从精简 Manifest 和惰性字段开始，但不能重新混淆状态。
2. **外部聊天导入不移入 P5。** 它不是核心 Harness 假设，却是 V2 已证明的完整产品能力；通过 P3-C 后置和来源 Adapter 独立 Gate 控制工作量，而不是删除或无限延期。

---

## 7. 希望架构师重点复审的内容

1. Grok Build 安全上限能否有效控制复杂度，是否仍有应移入 P5 的机制？
2. 七个 Module 作为同一 Rust 进程内的逻辑权威后，是否仍存在过浅 Interface 或重复权威？
3. 补全后的 Bridge Spike 是否覆盖所有会推翻整体架构的技术假设，20ms p95 初始预算是否合理？
4. Capsule 的唯一权威、Verbatim/Narrative 分区和重新推导 Required Context 是否封住摘要链漂移？
5. Slice Trigger 与用户否决能否平衡抗失真、交接成本和 cache miss？
6. Flaky 状态、确认重跑和 Verification Budget 是否适合有发布日期压力的现实开发？
7. Game Verification Pack 的可见性是 Tier 3、但最小项目 Adapter 纳入 P2 日程，这个切分是否合理？
8. P3-A/B/C 是否有效削平隆起段，同时仍然交付完整产品能力？
9. Extension 四态保留、Manifest 字段惰性加深是否仍有不必要复杂度？

本轮希望得到的不只是安全漏洞列表，也包括复杂度审计：任何安全机制如果超过 Grok Build、没有直接保护核心开发流，应明确建议删除、简化或移动到 P5。
